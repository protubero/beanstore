protect main branch and work with feature branches in the future.
More Automated Tests
Auto-Release, Protected Main branch
fix architecture tests

kryo Dictionary

# KeyValue Store
	
refactor Plugin mechanism	
------------------------------------- 0.8.5

# Links

class Task  {
	@Reference(autoDelete=true)
	Task subTask
}

class Task  {
	@BackReference(fieldName="subTask")
	Task mainTask 
}

class level Annotation IgnoreEntityField
class BackReference {
}

class ReferenceSerializer {
}

class ReferenceManager implements TransactionListener {
}


LinkAndPartEntity
	alias
	id
	info
	seqNo NULLS
	ref
	
Links

Parts

PartList<LinkAndPartEntity> partList = store.partsOf(doc)
tx.update(partList)

Links links = LinkHandler.linksOf(doc, store)
LinkHandler.unLink(tx, link)
LinkHandler.link(tx, source, target, "");

tx.appendPart(source, target)
tx.insertPartAt(source, target, idx) // concurrent?

storeState.partInfos().filter(pi -> pi.source.alias == 'xx')

------------------------------------- 0.8.6

Lock File

Generated Entities
	Yaml
	JavaPoet & Code Generation Plugin
	https://github.com/drewctaylor/javapoet-maven-plugin
	

#File Reference Manager
class FileRef {
	String fileName;
	String description;
	String originalFileName	
	
}

class FileManager implements RefSerializer, TransactionListener .. {

}


Ignore Annotation / Transient



